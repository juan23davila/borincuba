/**
 * Manager of functions from all pages
 */

var interfaceContent; // Variable global para concatenar contenido

//Concatena contenido
function addContent(content){
    interfaceContent = interfaceContent + content + "\n";
}